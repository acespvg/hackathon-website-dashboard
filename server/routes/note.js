// const express = require('express');
// const router = express.Router();
// const noteController = require('../controllers/noteController');
// const authMiddleware = require('../middleware/authMiddleware'); // Assuming you have some auth middleware

// // Ensure that only authenticated users can access the routes
// router.use(authMiddleware);

// // Define routes for notes
// router.post('/', noteController.createNote); // Create a new note
// router.get('/:ticketId', noteController.getNotesForTicket); // Get all notes for a specific ticket
// router.get('/note/:id', noteController.getNoteById); // Get a specific note by ID
// router.put('/:id', noteController.updateNote); // Update a note
// router.delete('/:id', noteController.deleteNote); // Delete a note

// module.exports = router;
